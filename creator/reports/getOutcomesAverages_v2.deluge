map getOutcomesAverages()
{
	returnMap = Map();

	// =====================================================
	// LOAD ELIGIBLE PARTICIPANTS
	// =====================================================

	eligibleParticipants = Participant_Master[
		Outcome_Report_Eligible.contains("Yes")
	];

	eligibleIDs = List();

	for each p in eligibleParticipants
	{
		preRec = Vitality_Snapshots[
			Participant == p &&
			Review_Status == "Approved" &&
			Assessment_Phase == "Pre"
		];

		postRec = Vitality_Snapshots[
			Participant == p &&
			Review_Status == "Approved" &&
			Assessment_Phase == "Post"
		];

		if(preRec.count() > 0 && postRec.count() > 0)
		{
			eligibleIDs.add(p.ID);
		}
	}

	returnMap.put("participantCount",eligibleIDs.size());

	// =====================================================
	// LOAD OUTCOME REPORT OVERRIDES
	// =====================================================

	overrideMap = Map();

	overrides = Outcome_Report_Overrides[
		Decision == "Exclude"
	];

	for each o in overrides
	{
		key = o.Participant.ID.toString() + "|" + o.Measure;
		overrideMap.put(key,true);
	}

	// =====================================================
	// LOAD SNAPSHOTS
	// =====================================================

	preSnapshots = Vitality_Snapshots[
		Review_Status == "Approved" &&
		Assessment_Phase == "Pre" &&
		Participant in eligibleIDs
	];

	postSnapshots = Vitality_Snapshots[
		Review_Status == "Approved" &&
		Assessment_Phase == "Post" &&
		Participant in eligibleIDs
	];
		// =====================================================
	// MOOD QUOTIENT
	// =====================================================

	mqPre = List();
	mqPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Mood_Quotient";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Mind_Flow_Score != null &&
				post.Mind_Flow_Score != null
			)
			{
				mqPre.add(pre.Mind_Flow_Score);
				mqPost.add(post.Mind_Flow_Score);
			}
		}
	}

	returnMap.put(
		"Mood_Quotient",
		thisapp.calculateStatistics(
			mqPre,
			mqPost,1
		)
	);
	// =====================================================
	// COGNITIVE CLARITY
	// =====================================================

	ccPre = List();
	ccPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Cognitive_Clarity";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Cognitive_Clarity != null &&
				post.Cognitive_Clarity != null
			)
			{
				ccPre.add(pre.Cognitive_Clarity);
				ccPost.add(post.Cognitive_Clarity);
			}
		}
	}

	returnMap.put(
		"Cognitive_Clarity",
		thisapp.calculateStatistics(
			ccPre,
			ccPost,1
		)
	);

    	// =====================================================
	// EMOTIONAL RESILIENCE
	// =====================================================

	erPre = List();
	erPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Emotional_Resilience";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Emotional_Resilience != null &&
				post.Emotional_Resilience != null
			)
			{
				erPre.add(pre.Emotional_Resilience);
				erPost.add(post.Emotional_Resilience);
			}
		}
	}

	returnMap.put(
		"Emotional_Resilience",
		thisapp.calculateStatistics(erPre,erPost,1)
	);

	// =====================================================
	// SOMATIC BALANCE
	// =====================================================

	sbPre = List();
	sbPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Somatic_Balance";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Somatic_Balance != null &&
				post.Somatic_Balance != null
			)
			{
				sbPre.add(pre.Somatic_Balance);
				sbPost.add(post.Somatic_Balance);
			}
		}
	}

	returnMap.put(
		"Somatic_Balance",
		thisapp.calculateStatistics(sbPre,sbPost,1)
	);

	// =====================================================
	// MEANING & ENGAGEMENT
	// =====================================================

	mePre = List();
	mePost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Meaning_Engagement";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Meaning_Engagement != null &&
				post.Meaning_Engagement != null
			)
			{
				mePre.add(pre.Meaning_Engagement);
				mePost.add(post.Meaning_Engagement);
			}
		}
	}

	returnMap.put(
		"Meaning_Engagement",
		thisapp.calculateStatistics(mePre,mePost,1)
	);

	// =====================================================
	// FUTURE ORIENTATION
	// =====================================================

	foPre = List();
	foPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Future_Orientation";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Future_Orientation != null &&
				post.Future_Orientation != null
			)
			{
				foPre.add(pre.Future_Orientation);
				foPost.add(post.Future_Orientation);
			}
		}
	}

	returnMap.put(
		"Future_Orientation",
		thisapp.calculateStatistics(foPre,foPost,1)
	);

    	// =====================================================
	// BODY VITALITY
	// =====================================================

	bvPre = List();
	bvPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Body_Vitality";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Body_Vitality_Score != null &&
				post.Body_Vitality_Score != null
			)
			{
				bvPre.add(pre.Body_Vitality_Score);
				bvPost.add(post.Body_Vitality_Score);
			}
		}
	}

	returnMap.put(
		"Body_Vitality",
		thisapp.calculateStatistics(bvPre,bvPost,1)
	);

	// =====================================================
	// CARDIOVASCULAR RESILIENCE
	// =====================================================

	cvPre = List();
	cvPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Cardiovascular_Resilience";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Cardiovascular_Resilience != null &&
				post.Cardiovascular_Resilience != null
			)
			{
				cvPre.add(pre.Cardiovascular_Resilience);
				cvPost.add(post.Cardiovascular_Resilience);
			}
		}
	}

	returnMap.put(
		"Cardiovascular_Resilience",
		thisapp.calculateStatistics(cvPre,cvPost,1)
	);

	// =====================================================
	// METABOLIC RESILIENCE
	// =====================================================

	mrPre = List();
	mrPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Metabolic_Resilience";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Metabolic_Resilience != null &&
				post.Metabolic_Resilience != null
			)
			{
				mrPre.add(pre.Metabolic_Resilience);
				mrPost.add(post.Metabolic_Resilience);
			}
		}
	}

	returnMap.put(
		"Metabolic_Resilience",
		thisapp.calculateStatistics(mrPre,mrPost,1)
	);

	// =====================================================
	// INFLAMMATION REGULATION
	// =====================================================

	irPre = List();
	irPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Inflammation_Regulation";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Inflammation_Regulation != null &&
				post.Inflammation_Regulation != null
			)
			{
				irPre.add(pre.Inflammation_Regulation);
				irPost.add(post.Inflammation_Regulation);
			}
		}
	}

	returnMap.put(
		"Inflammation_Regulation",
		thisapp.calculateStatistics(irPre,irPost,1)
	);
		// =====================================================
	// VITAMIN STATUS
	// =====================================================

	vsPre = List();
	vsPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Vitamin_Status";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Vitamin_Status != null &&
				post.Vitamin_Status != null
			)
			{
				vsPre.add(pre.Vitamin_Status);
				vsPost.add(post.Vitamin_Status);
			}
		}
	}

	returnMap.put(
		"Vitamin_Status",
		thisapp.calculateStatistics(vsPre,vsPost,1)
	);

	// =====================================================
	// PHYSICAL RESILIENCE
	// =====================================================

	prPre = List();
	prPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Physical_Resilience";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Physical_Resilience != null &&
				post.Physical_Resilience != null
			)
			{
				prPre.add(pre.Physical_Resilience);
				prPost.add(post.Physical_Resilience);
			}
		}
	}

	returnMap.put(
		"Physical_Resilience",
		thisapp.calculateStatistics(prPre,prPost,1)
	);

	// =====================================================
	// BELLY BALANCE
	// =====================================================

	bbPre = List();
	bbPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Belly_Balance";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Belly_Balance != null &&
				post.Belly_Balance != null
			)
			{
				bbPre.add(pre.Belly_Balance);
				bbPost.add(post.Belly_Balance);
			}
		}
	}

	returnMap.put(
		"Belly_Balance",
		thisapp.calculateStatistics(bbPre,bbPost,1)
	);
	
		// =====================================================
	// SYSTOLIC BP
	// =====================================================

	sbpPre = List();
	sbpPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Systolic_BP";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Systolic_BP != null &&
				post.Systolic_BP != null
			)
			{
				sbpPre.add(pre.Systolic_BP);
				sbpPost.add(post.Systolic_BP);
			}
		}
	}

	returnMap.put(
		"Systolic_BP",
		thisapp.calculateStatistics(sbpPre,sbpPost,-1)
	);

	// =====================================================
	// DIASTOLIC BP
	// =====================================================

	dbpPre = List();
	dbpPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Diastolic_BP";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Diastolic_BP != null &&
				post.Diastolic_BP != null
			)
			{
				dbpPre.add(pre.Diastolic_BP);
				dbpPost.add(post.Diastolic_BP);
			}
		}
	}

	returnMap.put(
		"Diastolic_BP",
		thisapp.calculateStatistics(dbpPre,dbpPost,-1)
	);

	// =====================================================
	// RESTING PULSE
	// =====================================================

	rpPre = List();
	rpPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Resting_Pulse";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Resting_Pulse != null &&
				post.Resting_Pulse != null
			)
			{
				rpPre.add(pre.Resting_Pulse);
				rpPost.add(post.Resting_Pulse);
			}
		}
	}

	returnMap.put(
		"Resting_Pulse",
		thisapp.calculateStatistics(rpPre,rpPost,-1)
	);

	// =====================================================
	// APOB
	// =====================================================

	apobPre = List();
	apobPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|ApoB";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.ApoB != null &&
				post.ApoB != null
			)
			{
				apobPre.add(pre.ApoB);
				apobPost.add(post.ApoB);
			}
		}
	}

	returnMap.put(
		"ApoB",
		thisapp.calculateStatistics(apobPre,apobPost,-1)
	);

	// =====================================================
	// LDL
	// =====================================================

	ldlPre = List();
	ldlPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|LDL";

			if(
				overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.LDL != null &&
				post.LDL != null
			)
			{
				ldlPre.add(pre.LDL);
				ldlPost.add(post.LDL);
			}
		}
	}

	returnMap.put(
		"LDL",
		thisapp.calculateStatistics(ldlPre,ldlPost,-1)
	);
	
		// =====================================================
	// HDL
	// =====================================================

	hdlPre = List();
	hdlPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|HDL";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.HDL != null &&
				post.HDL != null)
			{
				hdlPre.add(pre.HDL);
				hdlPost.add(post.HDL);
			}
		}
	}

	returnMap.put(
		"HDL",
		thisapp.calculateStatistics(hdlPre,hdlPost,1)
	);

	// =====================================================
	// TRIGLYCERIDES
	// =====================================================

	tgPre = List();
	tgPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Triglycerides";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Triglycerides != null &&
				post.Triglycerides != null)
			{
				tgPre.add(pre.Triglycerides);
				tgPost.add(post.Triglycerides);
			}
		}
	}

	returnMap.put(
		"Triglycerides",
		thisapp.calculateStatistics(tgPre,tgPost,-1)
	);

	// =====================================================
	// HBA1C
	// =====================================================

	hba1cPre = List();
	hba1cPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|HbA1c";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.HbA1c != null &&
				post.HbA1c != null)
			{
				hba1cPre.add(pre.HbA1c);
				hba1cPost.add(post.HbA1c);
			}
		}
	}

	returnMap.put(
		"HbA1c",
		thisapp.calculateStatistics(hba1cPre,hba1cPost,-1)
	);

	// =====================================================
	// HSCRP
	// =====================================================

	hscrpPre = List();
	hscrpPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|hsCRP";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.hsCRP != null &&
				post.hsCRP != null)
			{
				hscrpPre.add(pre.hsCRP);
				hscrpPost.add(post.hsCRP);
			}
		}
	}

	returnMap.put(
		"hsCRP",
		thisapp.calculateStatistics(hscrpPre,hscrpPost,-1)
	);

	// =====================================================
	// VITAMIN D
	// =====================================================

	vdPre = List();
	vdPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Vitamin_D";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Vitamin_D != null &&
				post.Vitamin_D != null)
			{
				vdPre.add(pre.Vitamin_D);
				vdPost.add(post.Vitamin_D);
			}
		}
	}

	returnMap.put(
		"Vitamin_D",
		thisapp.calculateStatistics(vdPre,vdPost,1)
	);
	
		// =====================================================
	// VITAMIN B12
	// =====================================================

	vb12Pre = List();
	vb12Post = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Vitamin_B12";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Vitamin_B12 != null &&
				post.Vitamin_B12 != null)
			{
				vb12Pre.add(pre.Vitamin_B12);
				vb12Post.add(post.Vitamin_B12);
			}
		}
	}

	returnMap.put(
		"Vitamin_B12",
		thisapp.calculateStatistics(vb12Pre,vb12Post,1)
	);

	// =====================================================
	// WEIGHT
	// =====================================================

	weightPre = List();
	weightPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Weight";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Weight != null &&
				post.Weight != null)
			{
				weightPre.add(pre.Weight);
				weightPost.add(post.Weight);
			}
		}
	}

	returnMap.put(
		"Weight",
		thisapp.calculateStatistics(weightPre,weightPost,-1)
	);

	// =====================================================
	// BMI
	// =====================================================

	bmiPre = List();
	bmiPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|BMI";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.BMI != null &&
				post.BMI != null)
			{
				bmiPre.add(pre.BMI);
				bmiPost.add(post.BMI);
			}
		}
	}

	returnMap.put(
		"BMI",
		thisapp.calculateStatistics(bmiPre,bmiPost,-1)
	);

	// =====================================================
	// VISCERAL FAT
	// =====================================================

	vfPre = List();
	vfPost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Visceral_Fat";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Visceral_Fat != null &&
				post.Visceral_Fat != null)
			{
				vfPre.add(pre.Visceral_Fat);
				vfPost.add(post.Visceral_Fat);
			}
		}
	}

	returnMap.put(
		"Visceral_Fat",
		thisapp.calculateStatistics(vfPre,vfPost,-1)
	);

	// =====================================================
	// BODY AGE
	// =====================================================

	bodyAgePre = List();
	bodyAgePost = List();

	for each participant in eligibleParticipants
	{
		if(eligibleIDs.contains(participant.ID))
		{
			pre = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Pre"
			];

			post = Vitality_Snapshots[
				Participant == participant &&
				Review_Status == "Approved" &&
				Assessment_Phase == "Post"
			];

			key = participant.ID.toString() + "|Body_Age";

			if(overrideMap.get(key) == null &&
				pre.count() > 0 &&
				post.count() > 0 &&
				pre.Body_Age != null &&
				post.Body_Age != null)
			{
				bodyAgePre.add(pre.Body_Age);
				bodyAgePost.add(post.Body_Age);
			}
		}
	}

	returnMap.put(
		"Body_Age",
		thisapp.calculateStatistics(bodyAgePre,bodyAgePost,-1)
	);
	return returnMap;
}